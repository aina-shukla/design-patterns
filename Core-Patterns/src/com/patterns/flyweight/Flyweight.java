package com.patterns.flyweight;

public class Flyweight {

	// structural design pattern to save memory by reusing object
	
	public static void main(String s[]) {
		PaintApp app = new PaintApp();
		app.render(10);
	}
}
